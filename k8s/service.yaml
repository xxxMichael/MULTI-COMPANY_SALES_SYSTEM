apiVersion: v1
kind: Service
metadata:
  name: multi-company-service
  namespace: multi-company
spec:
  type: NodePort
  selector:
    app: multi-company
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
      nodePort: 30090
